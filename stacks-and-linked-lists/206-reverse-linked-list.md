# Program: Reverse linked list
[Link](https://leetcode.com/problems/reverse-linked-list/)

## Approach
To reverse a linked list, it must be traversed and then the next pointer must
be made to point to the previous node (which must be stored).

## Complexity
- Time: $O(n)$
- Space: $O(1)$